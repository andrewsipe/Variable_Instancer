# Variable_Instancer dependencies
# Tool for extracting static font instances from variable fonts.

# Core dependencies (required by all scripts)
fonttools>=4.40.0
rich>=13.0.0

# No additional dependencies beyond core
